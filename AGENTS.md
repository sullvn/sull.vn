Keep all AI agent configurations in sync between harnesses

When adding agent skills, add them to the harness-agnostic agents directory and integrate them as required to harness-specific configuration locations. Review the repo to determine which harnesses these may be.
